# Reconcile sweep: every 15 minutes (cron lives in the calling stub), compare
# Linear card states against pipeline reality (runs, PRs, verdicts) and nudge
# whatever is stuck. The webhook path is primary; this sweep is the backstop
# that makes a missed webhook a 15-minute delay instead of a silent stall.
#
# Reusable workflow: permissions live in the calling stub (contents: read,
# actions: write). Job-level concurrency stays here.
name: Reconcile (reusable)

on:
  workflow_call:
    inputs:
      pipeline_ref:
        description: >-
          bureau-pipeline ref for the INTERNAL checkouts (scripts, briefs,
          standards). REQUIRED since DRE-2689: a caller must pass the same
          ref it pins in `uses:`, or it runs channel workflow YAML over
          another ref's scripts — pinned to everything that counts refs,
          and not actually pinned (DRE-2026).
        type: string
        required: true
      max_wip:
        description: WIP cap for the dependency gate's auto-promotion
        type: string
        required: false
        default: "8"
      intake_hold:
        description: >-
          THE PEN'S SWITCH (DRE-3035). Set it — ideally to the date you set
          it — and the age-out moves nothing; the sweep prints one line per
          pass saying the pen is closed and how much is behind it. Clear it
          and the age-out resumes. `groomer.yml` takes the same switch, so a
          closed pen holds both things that move a card out of Intake. Set it
          before a cutover run; clear it when the front door is proven.
        type: string
        required: false
        default: ""
      intake_max_age_minutes:
        description: >-
          How long a card may sit in Intake before the sweep escalates it to
          Green Light. EMPTY means the lane contract's own window for Intake
          (48h) — the number is declared in config/lane-contract.json and a
          default restated here would be the copy that drifts. Widening it is
          the deliberate operator act with an end date the cutover runbook
          describes.
        type: string
        required: false
        default: ""
      intake_escalation_cap:
        description: >-
          How many aged Intake cards ONE sweep may move. Empty means
          `intake_controls.DEFAULT_CAP`. Every product repo's sweep reads the
          same board, so the fleet-wide rate is a multiple of this.
        type: string
        required: false
        default: ""
    secrets:
      ANTHROPIC_API_KEY:
        required: false
      CLAUDE_CODE_OAUTH_TOKEN:
        required: false
      LINEAR_API_KEY:
        required: true
      BUREAU_APP_ID:
        required: true
      BUREAU_APP_PRIVATE_KEY:
        required: true
      BUREAU_QA_APP_ID:
        required: false
      BUREAU_QA_APP_PRIVATE_KEY:
        required: false

jobs:
  sweep:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    concurrency:
      group: reconcile
      cancel-in-progress: false
    steps:
      - name: Mint bot token
        id: app
        uses: actions/create-github-app-token@v3
        with:
          app-id: ${{ secrets.BUREAU_APP_ID }}
          private-key: ${{ secrets.BUREAU_APP_PRIVATE_KEY }}

      - uses: actions/checkout@v7

      - uses: actions/checkout@v7
        with:
          repository: dreadnought-foundry/bureau-pipeline
          ref: ${{ inputs.pipeline_ref || 'main' }}
          path: .bureau-pipeline

      - name: Sweep
        env:
          LINEAR_API_KEY: ${{ secrets.LINEAR_API_KEY }}
          GH_TOKEN: ${{ steps.app.outputs.token }}
          # Workflow-dispatch needs actions:write, which the calling stub
          # grants to the workflow's own token — the App token gets HTTP 403
          # on the dispatch API (the silent DRE-1254 stall, 2026-06-12).
          # reconcile.py's gh_dispatch() swaps this in for `gh workflow run`.
          GH_DISPATCH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
          MAX_WIP: ${{ inputs.max_wip }}
          # The operator's three controls on Intake (DRE-3035), threaded from
          # the caller's inputs and never assigned a literal here: a per-repo
          # value baked into the shared channel is how one repo's cutover
          # window becomes everyone's. Each is empty-safe in the script — an
          # unset input interpolates to "" and a bare int("") would turn a
          # window question into a red sweep across the fleet.
          INTAKE_HOLD: ${{ inputs.intake_hold }}
          INTAKE_MAX_AGE_MINUTES: ${{ inputs.intake_max_age_minutes }}
          INTAKE_ESCALATION_CAP: ${{ inputs.intake_escalation_cap }}
        run: |
          # Cards carry `**Repo:** <slug>` — reconcile.py compares against the
          # lowercase repo basename.
          REPO_SLUG=$(basename "$GITHUB_REPOSITORY" | tr '[:upper:]' '[:lower:]')
          export REPO_SLUG
          python3 .bureau-pipeline/scripts/reconcile.py
